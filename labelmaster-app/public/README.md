# public 정적 파일

여기에 둔 파일은 빌드 시 루트(`/`) 경로로 그대로 제공됩니다.

## 튜토리얼 라벨 샘플 이미지

첫 실행 튜토리얼 1단계에 쓰이는 라벨 사진을 아래 이름으로 넣어주세요:

```
public/tutorial-label.jpg
```

- 코드 참조: `src/screens/Intro.tsx`의 `SAMPLE_IMG = '/tutorial-label.jpg'`
- 권장: 세로형(원재료표가 또렷하게 보이는 제품 뒷면 사진), 너비 720px 내외
- 파일이 없으면 줄무늬 플레이스홀더가 대신 표시됩니다(빌드는 정상).
- 다른 파일명을 쓰려면 `SAMPLE_IMG` 값을 같이 바꿔주세요.
```
